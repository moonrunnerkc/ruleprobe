/**
 * Local type re-exports for the fingerprint module and its
 * structural-features submodules. Lets nested files import from a
 * nearby module instead of reaching up several directory levels.
 */

export type { PatternTopic, RawFileVector } from '../../types.js';
